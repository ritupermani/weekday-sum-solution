# Weekday Sum Solution

This project solves a JavaScript problem where date-based values are grouped by weekday.

## ✅ Features
- Aggregates values per weekday (Mon–Sun)
- Handles missing weekdays using the mean of adjacent days
- Efficient O(N) solution
- Fully unit-tested with Jest

## 📂 Input Format
```js
{
  'YYYY-MM-DD': number
}
